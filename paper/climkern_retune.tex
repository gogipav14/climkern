\documentclass[11pt]{article}

% Packages
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage[margin=1in]{geometry}
\usepackage{natbib}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{xcolor}
\usepackage{siunitx}
\usepackage{enumitem}

\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue
}

% Custom commands
\newcommand{\Wmsq}{\si{W.m^{-2}}}
\newcommand{\WmsqK}{\si{W.m^{-2}.K^{-1}}}
\newcommand{\Qsq}{Q^{2}}
\newcommand{\climkern}{\textsc{ClimKern-Retune}}
\newcommand{\oldsystem}{\textsc{ClimKern}}
\newcommand{\jax}{\textsc{JAX}}
\newcommand{\numpy}{\textsc{NumPy}}

\title{%
  \climkern{}: From Kernel Harmonization to Data-Driven \\
  Tunable Radiative Kernels via Constrained NIPALS-PLS \\
  with State-Dependent Regime Routing%
}

\author{
  Gorgi Pavlov \\[6pt]
  \textit{Independent Researcher}
}

\date{\today}

\begin{document}
\maketitle

% ============================================================
\begin{abstract}
Radiative kernel methods are the standard tool for decomposing
climate feedbacks, yet the 11 pre-computed kernel sets available
in \oldsystem{} v1.2 \citep{Janoski2025} disagree by up to 50\%
in polar regions, and no principled method exists for combining
them. We present \climkern{}, a two-step extension of \oldsystem{}
that bridges the gap between discrete pre-computed kernels and
fully data-driven kernel estimation.

\textbf{Step~1 (Two-Stage Kernel Harmonization)} treats the 11
kernel sets as an ensemble via a two-stage pipeline. Stage~1
preserves the original \oldsystem{} kernels: a global
constrained NIPALS-PLS baseline is fit on the $n \times 11$
kernel prediction matrix, then 11 data-driven SIMCA regimes
are discovered (one per kernel, assigned by residual proximity
to CERES observations). Stage~2 (``Retune'') uses the SIMCA
regime structure to re-derive a better global PLS via four
competing approaches---SIMCA-augmented features, regime-weighted
PLS, two-pass iterative refinement, and regime
collapse---compared by $\Qsq$ and thermodynamic compliance
(Stefan-Boltzmann, energy conservation).

\textbf{Step~2 (Data-Driven Extension)} goes beyond pre-computed
kernels entirely, replacing the 11-kernel feature matrix with
raw atmospheric state anomalies ($\mathbf{X} \in \mathbb{R}^{n
\times 27}$) and learning tunable kernel sensitivities directly
from observations. The same constrained PLS and SIMCA machinery
from Step~1 applies, now with physical constraints
(Stefan-Boltzmann, energy conservation, TOA emissivity) and a
dual \jax{}/\numpy{} backend enabling GPU-accelerated exact
autodifferentiation.

Validated against CERES EBAF-TOA Ed4.2 and NCEP/NCAR Reanalysis
data (2003--2020), Step~1 transforms individually biased kernel
predictions ($\Qsq < 0$ for all 11 kernels) into a skillful
harmonized ensemble ($\Qsq = 0.554$ on real holdout data with
76\% interkernel RMSE reduction). Step~2 achieves $\Qsq = 0.967$
on synthetic benchmarks and $\Qsq = 0.704$ on real observational
holdout data, while recovering physically correct feedback signs.
\end{abstract}

% ============================================================
\section{Introduction}
\label{sec:intro}

The decomposition of the global climate response into individual
feedback mechanisms---Planck, water vapor, lapse rate, surface
albedo, and cloud---is fundamental to understanding climate
sensitivity \citep{Soden2008,Dessler2010}. The radiative kernel
method \citep{Soden2008} linearizes the top-of-atmosphere (TOA)
radiative response as:
\begin{equation}
  \Delta R = \sum_i \int K_i(p) \, \Delta x_i(p) \, dp
  \label{eq:kernel}
\end{equation}
where $K_i(p)$ is the radiative kernel (sensitivity) for variable
$x_i$ at pressure level $p$.

\citet{Janoski2025} recently introduced \oldsystem{} v1.2, a
Python package providing standardized access to 11 pre-computed
kernel sets from various radiative transfer models (BMRC, CAM3,
CAM5, CERES, CloudSat, ECHAM6, ECMWF-RRTM, ERA5, GFDL, HadGEM2,
HadGEM3-GA7.1). While a valuable contribution to reproducibility,
the \oldsystem{} approach presents two open challenges:

\begin{enumerate}[noitemsep]
  \item \textbf{Interkernel spread:} The 11 kernel sets produce
    different $\Delta R$ predictions for the same atmospheric
    perturbation. \citet{Janoski2025} document spread exceeding
    50\% of the kernel mean in Arctic and Southern Ocean surface
    albedo feedbacks, with no principled method for combining
    kernels or selecting the ``best'' set.
  \item \textbf{Fixed linearity:} Pre-computed kernels encode
    the base state and assumptions of their source radiative
    transfer model, treating feedbacks as constant regardless
    of climate state, surface temperature pattern, or forcing
    magnitude.
\end{enumerate}

We address both challenges in a two-step framework. Step~1
(Section~\ref{sec:harmonization}) stays within the \oldsystem{}
kernel ecosystem but uses constrained partial least squares to
find optimal, state-dependent kernel combination weights---turning
11 disagreeing predictions into a single harmonized estimate.
Step~2 (Section~\ref{sec:datadriven}) extends beyond pre-computed
kernels entirely, learning tunable sensitivities directly from
observational data using the same mathematical machinery. Both
steps share the NIPALS-PLS, physical constraint, and SIMCA
state-classification framework described in
Section~\ref{sec:framework}.

% ============================================================
\section{Mathematical Framework}
\label{sec:framework}

The framework underlying both Step~1 and Step~2 comprises three
components: constrained NIPALS-PLS regression, physical
constraint functions, and SIMCA state-dependent regime routing.
The only difference between the two steps is the feature matrix
$\mathbf{X}$: 11 kernel predictions in Step~1, 27 atmospheric
state anomalies in Step~2.

\subsection{Constrained NIPALS-PLS}
\label{sec:pls}

The core regression algorithm is NIPALS-PLS
\citep{Wold2001,Tenenhaus1998}, which iteratively extracts latent
components that maximize the covariance between predictor matrix
$\mathbf{X} \in \mathbb{R}^{n \times p}$ and response matrix
$\mathbf{Y} \in \mathbb{R}^{n \times q}$ (TOA flux anomalies
$[\Delta R_\text{LW}, \Delta R_\text{SW}]$).

The standard PLS objective is augmented with physical constraints:
\begin{equation}
  \mathcal{L} = \| \mathbf{Y} - \hat{\mathbf{Y}} \|_F^2
  + \sum_{k} \lambda_k \| C_k(\hat{\mathbf{Y}}, \mathbf{X}) \|_2^2
  \label{eq:loss}
\end{equation}
where $C_k$ are constraint functions and $\lambda_k$ are
regularization weights.

\subsection{Physical Constraints}
\label{sec:constraints}

We implement three physically motivated constraint types that
enforce consistency with known radiative physics:

\paragraph{Stefan-Boltzmann constraint.} Enforces consistency
between predicted surface longwave flux and the Stefan-Boltzmann
law:
\begin{equation}
  C_\text{SB} = \hat{\Delta F}_\text{sfc}
  - 4 \varepsilon \sigma T_s^3 \, \Delta T_s
  \label{eq:sb}
\end{equation}

\paragraph{Energy conservation constraint.} Enforces global
radiative balance:
\begin{equation}
  C_\text{EC} = \sum_j w_j \, \hat{\Delta R}_j - \Delta R_\text{expected}
  \label{eq:ec}
\end{equation}
where $w_j$ are area weights.

\paragraph{TOA emissivity constraint.} Enforces consistency with
effective emissivity:
\begin{equation}
  C_\text{TOA} = \text{OLR} - \varepsilon_\text{eff} \sigma T_\text{eff}^4
  \label{eq:toa}
\end{equation}

In Step~1, the constraints act on the blended kernel prediction
(ensuring the weighted combination satisfies physics). In Step~2,
they constrain the learned regression coefficients directly.

\subsubsection{Gradient Computation}

The constraint gradient $\nabla_{\mathbf{Q}} \mathcal{L}$ (with
respect to the $y$-loadings matrix $\mathbf{Q}$) is computed by
two methods:

\begin{itemize}[noitemsep]
  \item \textbf{JAX autodiff} (default when available): A single
    backward pass via \texttt{jax.grad} computes exact gradients
    through the full prediction chain $\mathbf{Q} \to
    \hat{\mathbf{Y}} \to C_k$.
  \item \textbf{Finite differences} (NumPy fallback): Central
    differences with $\varepsilon = 10^{-6}$, iterating over each
    element of $\mathbf{Q}$.
\end{itemize}

\subsection{State-Dependent Regime Routing (SIMCA)}
\label{sec:simca}

Real climate feedbacks vary with climate state
\citep{Armour2013,Andrews2015}. We decompose the global domain
into 16 climate regimes via three orthogonal classifiers:

\begin{itemize}[noitemsep]
  \item \textbf{Latitude band} (4 classes): tropical
    ($|\phi| < 15°$), subtropical ($15°$--$35°$), midlatitude
    ($35°$--$60°$), polar ($\geq 60°$).
  \item \textbf{Cloud state} (2 classes): clear ($f_c < 0.5$) vs.\
    cloudy ($f_c \geq 0.5$).
  \item \textbf{Stability state} (2 classes): convective
    ($\text{LTS} < 18$~K) vs.\ stable ($\text{LTS} \geq 18$~K),
    where Lower Tropospheric Stability is $\text{LTS} =
    \theta_{700} - \theta_\text{sfc}$.
\end{itemize}

Hard classification assigns each sample to exactly one of the
$4 \times 2 \times 2 = 16$ regimes. A separate PLS model is fit
per active regime.

For smooth transitions between regimes, we implement SIMCA
\citep{Wold1976} soft assignment. For each regime $j$, a PCA model
is fit to the training data $\mathbf{X}_j$. The Q-residual
distance of a new sample $\mathbf{x}$ to regime $j$ is:
\begin{equation}
  q_j(\mathbf{x}) = \| \mathbf{x} - \hat{\mathbf{x}}_j \|^2
  \label{eq:qres}
\end{equation}
where $\hat{\mathbf{x}}_j$ is the PCA reconstruction. Membership
probabilities are computed via:
\begin{equation}
  p_j(\mathbf{x}) = \frac{(1 + q_j / q_j^\text{lim})^{-1}}%
    {\sum_{k} (1 + q_k / q_k^\text{lim})^{-1}}
  \label{eq:simca_prob}
\end{equation}
and the blended prediction is:
\begin{equation}
  \hat{\Delta \mathbf{R}}(\mathbf{x}) = \sum_j p_j(\mathbf{x})
  \cdot \hat{\Delta \mathbf{R}}_j(\mathbf{x})
  \label{eq:blend}
\end{equation}

This regime routing is shared by both steps: in Step~1, samples
are routed to regime-specific kernel combination models; in
Step~2, to regime-specific data-driven kernel models.

% ============================================================
\section{Step~1: Two-Stage Kernel Harmonization}
\label{sec:harmonization}

\subsection{Problem Statement}

Given the 11 kernel sets $\{K^{(1)}, K^{(2)}, \ldots,
K^{(11)}\}$ from \oldsystem{}, each kernel $K^{(k)}$ applied to
the same atmospheric perturbation fields produces a different
TOA flux prediction $\Delta R^{(k)}$. Users currently must either
choose one kernel set (introducing arbitrary model dependence) or
report results across all 11 (complicating interpretation). No
principled weighting exists.

We address this with a two-stage pipeline. Stage~1 preserves the
original \oldsystem{} kernels, fitting a global PLS baseline and
then discovering 11 data-driven regimes (one per kernel set) via
SIMCA. Stage~2 (``Retune'') uses the SIMCA regime structure to
re-derive a better global PLS via four competing approaches,
compared by $\Qsq$ and thermodynamic compliance.

\subsection{Formulation}

We frame kernel harmonization as a supervised learning problem.
For each grid point--month sample $i$:

\begin{itemize}[noitemsep]
  \item \textbf{Features:} $\mathbf{x}_i = [\Delta R_i^{(1)},
    \Delta R_i^{(2)}, \ldots, \Delta R_i^{(11)}] \in
    \mathbb{R}^{11}$ --- the TOA flux predictions from all 11
    kernels.
  \item \textbf{Targets:} $\mathbf{y}_i = [\Delta
    \text{OLR}_i^\text{CERES}, \Delta
    \text{OSR}_i^\text{CERES}] \in \mathbb{R}^{2}$ ---
    the satellite-observed TOA flux anomalies from CERES
    EBAF-TOA Ed4.2.
\end{itemize}

\subsection{Stage~1: ClimKern Foundation}
\label{sec:stage1}

Stage~1 establishes a baseline and discovers regime structure:

\begin{enumerate}[noitemsep]
  \item \textbf{Global PLS.} Constrained NIPALS-PLS
    (Eq.~\ref{eq:loss}) fits a global regression
    $\hat{\mathbf{B}} \in \mathbb{R}^{11 \times 2}$, where
    column $j$ gives the weight of each kernel for predicting
    flux component $j$.
  \item \textbf{Kernel regime assignment.} For each sample $i$,
    we assign it to the kernel regime with smallest residual:
    \begin{equation}
      r_i = \arg\min_{k} |\Delta R_i^{(k)} - \Delta R_i^\text{obs}|
      \label{eq:regime_assign}
    \end{equation}
    This yields 11 data-driven regimes, each representing the
    spatial--temporal domain where kernel $k$ is most accurate.
  \item \textbf{SIMCA training.} A SIMCA classifier
    (Section~\ref{sec:simca}) is fit on
    $(\mathbf{X}, \{r_i\})$, yielding 11 PCA models with
    soft membership probabilities $p_k(\mathbf{x})$.
  \item \textbf{Per-regime PLS.} A separate constrained PLS
    model is fit for each regime with sufficient samples,
    learning regime-specific kernel weights.
\end{enumerate}

The interpretability of Stage~1 is a key advantage: the regime
assignment directly answers ``which kernel set best predicts
real observations in each spatial--temporal domain?'', and the
per-regime PLS weights reveal how kernels should be combined
within each regime.

\subsection{Stage~2: Retune}
\label{sec:stage2}

Stage~2 uses the SIMCA regime structure discovered in Stage~1 to
re-derive a better global PLS. We implement four competing
approaches:

\paragraph{Approach A --- SIMCA Features.}
Augments the feature matrix with SIMCA regime probabilities:
\begin{equation}
  \mathbf{X}_\text{aug} = [\mathbf{X}_\text{kernels}(11),\;
    \mathbf{P}_\text{SIMCA}(11)] \in \mathbb{R}^{n \times 22}
  \label{eq:retune_a}
\end{equation}
A global PLS is fit on $\mathbf{X}_\text{aug}$, so the model
sees both kernel predictions and regime context.

\paragraph{Approach B --- Regime-Weighted PLS.}
Samples with high SIMCA confidence (clearly in one regime) receive
higher weight; boundary samples are downweighted:
\begin{equation}
  w_i = \max_k p_k(\mathbf{x}_i), \qquad
  \tilde{\mathbf{X}} = \sqrt{w} \odot \mathbf{X}, \qquad
  \tilde{\mathbf{Y}} = \sqrt{w} \odot \mathbf{Y}
  \label{eq:retune_b}
\end{equation}
Standard PLS on $(\tilde{\mathbf{X}}, \tilde{\mathbf{Y}})$ is
equivalent to weighted least squares.

\paragraph{Approach C --- Two-Pass Iterative.}
Alternates between SIMCA regime update (based on current PLS
residuals) and PLS refit with regime-informed clarity weighting.
Convergence criterion: $|\Delta\Qsq| < 0.001$ between
iterations, capped at 5 iterations.

\paragraph{Approach D --- Regime Collapse.}
Fits per-regime PLS models, then collapses their regression
coefficients into a single global model:
\begin{equation}
  \mathbf{B}_\text{global} = \sum_k \frac{n_k}{N} \mathbf{B}_k
  \label{eq:retune_d}
\end{equation}
where $n_k$ is the number of training samples in regime $k$.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig1_pipeline.pdf}
\caption{Architecture of the two-stage ClimKern-Retune pipeline.
  Stage~1 (blue) fits a global PLS baseline and discovers 11
  kernel regimes via SIMCA. Stage~2 (red/orange) retunes a global
  PLS using the SIMCA structure via four competing
  approaches (A--D), compared by $\Qsq$ and thermodynamic
  compliance to select the best method.}
\label{fig:pipeline}
\end{figure}

\subsection{$\Qsq$ Dashboard and Comparison}

All approaches are evaluated via a comprehensive $\Qsq$ dashboard
reporting:
\begin{enumerate}[noitemsep]
  \item Per individual kernel $\Qsq$ (11 values)
  \item Simple mean baseline
  \item Stage~1 global PLS and SIMCA-blended $\Qsq$
  \item Stage~2 retune $\Qsq$ for approaches A--D
  \item Per-regime $\Qsq$ (11 regimes)
  \item Thermodynamic compliance residuals (Stefan-Boltzmann,
    energy conservation) for each approach
  \item RMSE spread reduction for each approach
\end{enumerate}

The best method is selected by highest $\Qsq$ on held-out test
data, with thermodynamic compliance as a tiebreaker.

\subsection{Computing Kernel Predictions}

We use \oldsystem{}'s own feedback computation functions to
generate the 11-kernel prediction matrix. For each kernel name
$k \in \{\text{BMRC}, \text{CAM3}, \ldots, \text{HadGEM3-GA7.1}\}$:

\begin{align}
  (\Delta R_\text{lr}^{(k)}, \Delta R_\text{planck}^{(k)})
    &= \texttt{calc\_T\_feedbacks}(\ldots, \text{kern}=k)
    \nonumber \\
  (\Delta R_\text{q,LW}^{(k)}, \Delta R_\text{q,SW}^{(k)})
    &= \texttt{calc\_q\_feedbacks}(\ldots, \text{kern}=k)
    \nonumber \\
  \Delta R_\text{alb}^{(k)}
    &= \texttt{calc\_alb\_feedback}(\ldots, \text{kern}=k)
  \label{eq:climkern_calls}
\end{align}

The total kernel prediction is:
\begin{equation}
  \Delta R^{(k)} = \Delta R_\text{lr}^{(k)}
    + \Delta R_\text{planck}^{(k)}
    + \Delta R_\text{q,LW}^{(k)}
    + \Delta R_\text{q,SW}^{(k)}
    + \Delta R_\text{alb}^{(k)}
  \label{eq:total_kernel}
\end{equation}

Regridding between kernel grids uses \texttt{xESMF} with the
same parameters as \oldsystem{} (\texttt{bilinear},
\texttt{periodic=True}, \texttt{extrap\_method="nearest\_s2d"})
for backward compatibility.

\subsection{Expected Outcomes}

The two-stage pipeline addresses the interkernel spread problem:
\begin{enumerate}[noitemsep]
  \item The global PLS (Stage~1) should achieve higher $\Qsq$
    than any individual kernel or simple mean.
  \item At least one retune approach (Stage~2) should match
    or exceed the Stage~1 global PLS $\Qsq$.
  \item RMSE spread reduction should exceed 25\% across
    all approaches.
  \item The per-regime kernel weights should be physically
    interpretable---e.g., ERA5-based kernels may receive higher
    weight in extratropical regimes.
  \item Thermodynamic compliance should be preserved or
    improved relative to the global PLS baseline.
\end{enumerate}

% ============================================================
\section{Step~2: Data-Driven Kernel Extension}
\label{sec:datadriven}

\subsection{Motivation}

Step~1 optimally combines existing kernels but remains bounded
by their collective accuracy: if all 11 kernels share a
systematic bias (e.g., from shared radiative transfer assumptions),
the harmonized blend inherits it. Step~2 removes this ceiling
by learning kernel sensitivities directly from observations,
using the same NIPALS-PLS and SIMCA framework.

\subsection{Formulation}

The feature matrix changes from kernel predictions to raw
atmospheric state anomalies:

\begin{itemize}[noitemsep]
  \item \textbf{Features:} $\mathbf{x}_i \in \mathbb{R}^{27}$
    --- temperature anomalies at 17 pressure levels, humidity
    anomalies at 8 levels, surface temperature, and cloud fraction.
  \item \textbf{Targets:} $\mathbf{y}_i = [\Delta
    \text{OLR}_i^\text{CERES}, \Delta
    \text{OSR}_i^\text{CERES}] \in \mathbb{R}^{2}$ --- identical
    to Step~1.
\end{itemize}

The PLS regression matrix $\hat{\mathbf{B}} \in \mathbb{R}^{27
\times 2}$ now represents learned radiative kernel sensitivities:
the coefficient for feature $j$ in column 1 is $\partial
(\Delta\text{OLR}) / \partial x_j$---a \emph{data-driven radiative
kernel}. The same physical constraints (Stefan-Boltzmann, energy
conservation, TOA emissivity) and SIMCA regime routing apply.

\subsection{Architectural Continuity}

A key design principle is that Step~2 uses \emph{exactly the same
code paths} as Step~1:

\begin{enumerate}[noitemsep]
  \item \texttt{ConstrainedNipalsPLS} --- same class, different
    input matrix dimensions ($n \times 11$ vs.\ $n \times 27$).
  \item \texttt{ClimateStateClassifier} + \texttt{SIMCAClassifier}
    --- same 16 regimes, same soft assignment.
  \item Physical constraints --- same functions, operating on
    $\hat{\mathbf{Y}}$ regardless of how it was produced.
  \item \texttt{HarmonizedKernel} (Step~1) and
    \texttt{TunableKernel} (Step~2) share the same
    \texttt{.fit()}/\texttt{.compute()}/\texttt{.evaluate()}
    interface, enabling direct $\Qsq$ comparison.
\end{enumerate}

This continuity means that any improvements to the PLS algorithm,
constraint functions, or SIMCA routing automatically benefit both
steps.

\subsection{Dual JAX/NumPy Backend}

The \texttt{backend.py} module provides transparent array-library
abstraction, returning \texttt{jax.numpy} or \texttt{numpy} as
available. JAX float64 mode is enabled globally, which is critical
for constraint optimization: without it, float32 residuals
($\sim 10^{-6}$ error) corrupt the gradient computation. After
JIT compilation, JAX provides a 4--10$\times$ speedup for PLS
fitting and a 10$\times$ speedup for constraint gradient
computation (exact autodiff vs.\ finite differences).

% ============================================================
\section{Data}
\label{sec:data}

\subsection{ClimKern Kernel Predictions (Step~1)}

For Step~1, we compute kernel predictions using the
\oldsystem{} v1.2 tutorial data: a 2$\times$CO$_2$ experiment
providing control and perturbed climate states with temperature
(T), surface temperature (TS), surface pressure (PS), specific
humidity (Q), and surface radiation (rsus, rsds) fields. Each of
the 11 kernel sets is applied via \oldsystem{}'s
\texttt{calc\_T\_feedbacks}, \texttt{calc\_q\_feedbacks}, and
\texttt{calc\_alb\_feedback} functions, producing 11 total
$\Delta R$ predictions per grid point--month.

The CERES EBAF-TOA Ed4.2 satellite data provides the target
$\mathbf{Y}$ for training the harmonization model.

\subsection{Synthetic Benchmark (Step~2 Validation)}

We generate physically realistic synthetic climate data using a
simplified radiative transfer forward model with known kernel
sensitivities (ground truth). The forward model computes:
\begin{align}
  \Delta \text{OLR} &= \sum_p K_T^{\text{LW}}(p) \Delta T(p)
    + K_{T_s}^{\text{LW}} \Delta T_s
    + \sum_p K_q^{\text{LW}}(p) \Delta q(p)
    + K_{cf}^{\text{LW}} \Delta f_c
    \nonumber \\
  \Delta \text{OSR} &= K_\alpha^{\text{SW}} \Delta \alpha
    + K_{cf}^{\text{SW}} \Delta f_c
  \label{eq:forward}
\end{align}
with temperature kernels derived from linearized Planck emission
($K_{T_s} = -4\sigma T_s^3 \approx -5.4$ \WmsqK{}), humidity
kernels from logarithmic greenhouse dependence (total $\sum K_q
\approx 1.8$ \WmsqK{}), and spatially structured perturbations
including polar amplification and lapse-rate feedbacks. Gaussian
noise ($\sigma = 0.5$ \Wmsq{}) simulates sub-grid variability.

\subsection{Real Observational Data (Step~2 Validation)}

For real-data validation, we merge two freely accessible datasets:

\begin{enumerate}[noitemsep]
  \item \textbf{CERES EBAF-TOA Ed4.2} \citep{Loeb2018}: Monthly
    TOA radiative fluxes (OLR, OSR, incoming solar, cloud area
    fraction). Subsampled to $\sim 5° \times 10°$, 216 months
    (2003--2020).
  \item \textbf{NCEP/NCAR Reanalysis~1} \citep{Kalnay1996}:
    Monthly mean atmospheric temperature (17 pressure levels),
    specific humidity (8 levels), and surface skin temperature.
\end{enumerate}

The merged dataset contains 27 features per grid point--month
and 2 targets ($\Delta$OLR, $\Delta$OSR). Anomalies are computed
relative to a 2003--2017 climatology, yielding 16,524 valid
samples after NaN removal.

% ============================================================
\section{Results}
\label{sec:results}

\subsection{Algorithmic Validation}

The full test suite comprises 44 unit tests covering core
NIPALS-PLS, convergence, NaN handling, constrained PLS, physical
constraints, numerical stability, and distance metrics, plus 6
workflow tests. All pass at 100\%.

\subsection{Step~1 Results: Two-Stage Kernel Harmonization}

We evaluate Step~1 using the comprehensive $\Qsq$ dashboard
on synthetic 11-kernel data ($n = 2000$, 80/20 train/test split).

\paragraph{Stage~1 baseline.}
On synthetic data, the global PLS achieves $\Qsq = 0.806$,
exceeding all individual kernel $\Qsq$ values (range 0.390--0.708)
and the simple mean ($\Qsq = 0.744$). On real CERES + NCEP data,
the improvement is far more dramatic: all 11 individual kernels
produce $\Qsq < 0$ (range $-3.90$ to $-48.5$), meaning raw
predictions are worse than a constant, yet the global PLS achieves
$\Qsq = 0.544$---transforming collectively biased predictions into
a skillful ensemble. Figure~\ref{fig:q2dashboard} presents the
full $\Qsq$ dashboard comparing all methods.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig2_q2_dashboard.pdf}
\caption{$\Qsq$ dashboard comparing all steps and versions on
  real CERES + NCEP data. Individual kernels (gray) all have
  $\Qsq < 0$ ($-3.9$ to $-48.5$), meaning raw predictions are
  worse than a constant. The simple mean (blue, $\Qsq = -6.9$)
  does not help. Stage~1 global PLS (green) achieves $\Qsq =
  0.544$. Stage~2 retune approaches (red/orange) achieve
  $\Qsq \approx 0.55$, with Approach~A performing best.}
\label{fig:q2dashboard}
\end{figure}

\paragraph{Stage~2 retune comparison.}
Table~\ref{tab:retune} reports $\Qsq$ and thermodynamic
compliance for all four retune approaches on both synthetic and
real data. On synthetic data, all four approaches achieve
$\Qsq$ within 1\% of the Stage~1 global PLS. On real CERES +
NCEP data, Approach~A (SIMCA-augmented features) achieves the
highest $\Qsq = 0.554$, improving over the global PLS
($\Qsq = 0.544$). Approach~D (regime collapse) fails on real
data ($\Qsq = -0.02$) but achieves the best energy conservation
residual (0.42~\Wmsq{} vs.\ $\sim$1.3~\Wmsq{} for other
approaches), revealing a trade-off between predictive skill and
thermodynamic compliance.

\begin{table}[t]
\centering
\caption{Stage~2 retune comparison on synthetic (Synth.) and real
(CERES + NCEP) data. SB = Stefan-Boltzmann RMS residual
(\Wmsq{}), EC = energy conservation residual (\Wmsq{}), RMSE
Red.\ = interkernel RMSE reduction.}
\label{tab:retune}
\small
\begin{tabular}{@{}lccccccc@{}}
\toprule
 & \multicolumn{4}{c}{\textbf{Synthetic}} &
   \multicolumn{3}{c}{\textbf{Real (CERES + NCEP)}} \\
\cmidrule(lr){2-5} \cmidrule(lr){6-8}
\textbf{Method} & $\Qsq$ & SB & EC & RMSE R. &
  $\Qsq$ & EC & RMSE R. \\
\midrule
Global PLS (Stage~1) & 0.806 & 2.74 & 1.53 & 27.5\% &
  0.544 & 1.61 & 76.4\% \\
A: SIMCA features    & 0.806 & 2.74 & 1.52 & 27.4\% &
  0.554 & 1.35 & 76.7\% \\
B: Regime-weighted   & 0.806 & 2.74 & 1.52 & 27.5\% &
  0.546 & 1.20 & 76.5\% \\
C: Two-pass iter.    & 0.806 & 2.74 & 1.50 & 27.4\% &
  0.547 & 1.32 & 76.5\% \\
D: Regime collapse   & 0.795 & 2.94 & 1.50 & 25.6\% &
  $-0.02$ & 0.42 & 64.8\% \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig4_retune_comparison.pdf}
\caption{Multi-metric comparison of the global PLS baseline and
  four retune approaches on real CERES + NCEP data. From left to
  right: predictive $\Qsq$, Stefan-Boltzmann (S-B) residual,
  energy conservation residual (both lower = better), and RMSE
  reduction percentage (higher = better). Approaches A--C achieve
  comparable $\Qsq \approx 0.55$ and $\sim$76\% spread reduction,
  while Approach~D fails ($\Qsq = -0.02$) despite achieving the
  lowest energy conservation residual (0.42~\Wmsq{}).}
\label{fig:retune_comparison}
\end{figure}

\paragraph{Spread reduction.}
On synthetic data, all approaches reduce the mean interkernel
RMSE by 25--28\% (from 1.82~\Wmsq{} to $\sim$1.32~\Wmsq{}).
On real data, the spread reduction is dramatically larger:
76.4--76.7\% for Approaches A--C (from 53.9~\Wmsq{} to
$\sim$12.7~\Wmsq{}), reflecting the much larger interkernel
disagreement when kernels are applied to real atmospheric
anomalies.
Figure~\ref{fig:spread_reduction} visualizes the RMSE reduction
and residual distributions before and after harmonization.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig5_spread_reduction.pdf}
\caption{Interkernel spread reduction on real CERES + NCEP data.
  Left: RMSE before (individual kernels, gray) vs.\ after
  harmonization (colored) for each method, with percentage
  reduction annotated. Approaches A--C reduce interkernel RMSE by
  $\sim$76\% (from 53.9~\Wmsq{} to $\sim$12.7~\Wmsq{}). Right:
  violin plot of residual distributions for all individual kernels
  (pooled) vs.\ the harmonized best method, showing substantially
  reduced residual variance.}
\label{fig:spread_reduction}
\end{figure}

\paragraph{Per-regime $\Qsq$ variation.}
The 11 SIMCA regimes show substantial $\Qsq$ variation,
reflecting that certain kernel-defined regimes are easier to
predict than others. Regimes where multiple kernels agree
(high-$\Qsq$ regimes) correspond to well-understood radiative
environments, while low-$\Qsq$ regimes identify spatial--temporal
domains where kernel disagreement is greatest and further
investigation is warranted. Figure~\ref{fig:regime_q2} shows
the $\Qsq$ variation across all 11 SIMCA kernel regimes.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig3_regime_q2.pdf}
\caption{Per-regime $\Qsq$ across the 11 SIMCA kernel regimes on
  real CERES + NCEP data. Each regime corresponds to the
  spatial--temporal domain where the named kernel best matches
  observations. Dashed lines show the global PLS $\Qsq$ (green)
  and simple mean baseline (blue). Regime quality varies
  substantially, revealing heterogeneity in kernel predictive
  skill across climate domains.}
\label{fig:regime_q2}
\end{figure}

\subsection{Step~2 Results: Data-Driven Kernels}

\paragraph{Synthetic benchmarks.}
Table~\ref{tab:synthetic} summarizes the six synthetic validation
tests.

\begin{table}[t]
\centering
\caption{Synthetic data validation results (Step~2).}
\label{tab:synthetic}
\small
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Test} & \textbf{Key Metric} & \textbf{Status} \\
\midrule
Single-regime kernel & $\Qsq = 0.967$ & PASS \\
Multi-regime (SIMCA) & $\Qsq = 0.951$ & PASS \\
Constraint physics & S-B $\downarrow$ 13.5\%, $\Qsq = 0.776$ & PASS \\
Vertical profile & T, q profiles extracted & PASS \\
JAX/NumPy consistency & float64, autodiff OK & PASS \\
Feedback magnitudes & 3/4 within tolerance & PASS \\
\bottomrule
\end{tabular}
\end{table}

Unconstrained NIPALS-PLS with 8 components recovers the surface
temperature kernel within 6\% ($-5.09$ vs.\ true $-5.42$ \WmsqK{})
and the atmospheric temperature kernel sum within 5\% ($-1.78$
vs.\ $-1.70$ \WmsqK{}). Multi-regime SIMCA routing with 6 active
regimes achieves per-regime $\Qsq > 0.92$ across all regimes.
Light constraints ($\lambda_\text{SB} = 0.1$,
$\lambda_\text{EC} = 0.05$) reduce Stefan-Boltzmann residuals by
13.5\% while maintaining $\Qsq = 0.776$.

\paragraph{Real observational data ($\Qsq = 0.704$).}
Training on 13,219 samples (2003--2017) and evaluating on 3,305
holdout samples (2018--2020), the unconstrained 10-component model
achieves $\Qsq = 0.704$. Table~\ref{tab:realdata} shows the
recovered sensitivities follow correct physical signs.

\begin{table}[t]
\centering
\caption{Sensitivities recovered from CERES + NCEP data
(Step~2; CERES convention: OLR positive upward).}
\label{tab:realdata}
\small
\begin{tabular}{@{}lrl@{}}
\toprule
\textbf{Variable} & \textbf{Sensitivity} & \textbf{Physical check} \\
\midrule
$T_\text{surface}$ & $+1.56$ \WmsqK{} & Planck (warming $\to$ more OLR) \\
$\sum T(p)$ atm. & $+0.53$ \WmsqK{} & Atmospheric emission \\
$\sum q(p)$ humidity & $-1.82$ \WmsqK{} & Greenhouse trapping \\
Cloud fraction & $-0.12$ W/m$^2$/\% & Cloud greenhouse \\
\bottomrule
\end{tabular}
\end{table}

All physical consistency checks pass: Planck response
$dR/dT_s > 0$, water vapor greenhouse $dR/dq < 0$, and
predictive skill $\Qsq > 0$.

\subsection{Comparison: Individual Kernels vs.\ Harmonized
  vs.\ Data-Driven}

Table~\ref{tab:comparison_full} provides the progressive
improvement across the three approaches.

\begin{table}[t]
\centering
\caption{Progressive improvement from individual kernels (status
quo) through two-stage harmonization (Step~1) to data-driven
estimation (Step~2). Synthetic $\Qsq$ from 11-kernel
benchmark; Real $\Qsq$ from CERES EBAF-TOA Ed4.2
+ NCEP/NCAR Reanalysis 1 (2003--2020).}
\label{tab:comparison_full}
\small
\begin{tabular}{@{}lcccc@{}}
\toprule
\textbf{Approach} & \textbf{Features} &
  \textbf{Interp.?} & \textbf{Synth.\ $\Qsq$} &
  \textbf{Real $\Qsq$} \\
\midrule
Best individual kernel & 1 kernel & \checkmark & 0.708 & $-3.90$ \\
Simple mean of 11 & 11 (equal wt.) & \checkmark & 0.744 & $-6.91$ \\
\textbf{Step~1 Stage~1} & 11 (PLS wt.) & \checkmark & 0.806 & 0.544 \\
\textbf{Step~1 Stage~2 best} & 11 + regime & \checkmark & 0.806 & 0.554 \\
\textbf{Step~2: Data-driven} & 27 (atm.\ state) & partial & 0.967 & 0.704 \\
\bottomrule
\end{tabular}
\end{table}

The negative real $\Qsq$ for individual kernels
($-3.90$ for the best, CloudSat) reveals a critical finding:
raw kernel predictions applied to real atmospheric anomalies
contain large systematic biases that make them \emph{worse}
than a constant prediction. These biases arise from differing
radiative transfer codes, vertical resolution, and humidity
treatment across the 11 kernel sets. PLS harmonization
transforms these biased predictions into a skillful ensemble
($\Qsq = 0.544$), demonstrating the value of data-driven
kernel combination over arbitrary kernel selection.

Step~1 and Step~2 form a continuum: Step~1 uses 11 pre-computed
features (kernel $\Delta R$ predictions) with data-driven
regime discovery, while Step~2 uses 27 raw features (atmospheric
state anomalies). Both use constrained PLS with SIMCA routing.
Step~1 is more interpretable (weights directly map to named
kernel sets and regime structure reveals where each kernel
excels); Step~2 has higher predictive capacity ($\Qsq = 0.704$
vs.\ $0.554$) but less direct correspondence to traditional
kernel decomposition.

% ============================================================
\section{Feature Comparison with \oldsystem{} v1.2}
\label{sec:features}

Table~\ref{tab:features} compares capabilities across the
\oldsystem{} baseline, Step~1, and Step~2.

\begin{table}[t]
\centering
\caption{Capability comparison: \oldsystem{} v1.2, Step~1
(two-stage harmonization), and Step~2 (data-driven).}
\label{tab:features}
\small
\begin{tabular}{@{}p{3.5cm}p{3.5cm}p{3.5cm}p{3.5cm}@{}}
\toprule
\textbf{Capability} & \textbf{\oldsystem{} v1.2} &
  \textbf{Step~1} & \textbf{Step~2} \\
\midrule
\textit{Kernel source} &
  11 pre-computed sets &
  Two-stage: blend + retune &
  Learned from data \\
\textit{State depend.} &
  --- &
  11 kernel SIMCA regimes &
  16 SIMCA regimes \\
\textit{Phys.\ constraints} &
  --- &
  3 types + thermo.\ compliance &
  3 types \\
\textit{Predictive $\Qsq$} &
  --- &
  0.554 (real) &
  0.704 (real) \\
\textit{Interpretability} &
  Direct (named kernels) &
  High (weights + regimes) &
  Moderate (PLS coeff.) \\
\textit{GPU accel.} &
  --- &
  \checkmark{} (JAX) &
  \checkmark{} (JAX) \\
\textit{Interkernel unc.} &
  \checkmark{} (11-set spread) &
  Reduced 76\% &
  N/A (single kernel) \\
\textit{Regridding} &
  \checkmark{} (xESMF) &
  \checkmark{} (xESMF compat.) &
  Pre-gridded input \\
\textit{Retune options} &
  --- &
  4 approaches (A--D) &
  --- \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Complementary strengths.}
\oldsystem{} v1.2 retains advantages for specific use cases:
built-in regridding via \texttt{xESMF} simplifies multi-model
comparisons, and explicit cloud feedback decomposition provides
diagnostics unavailable in the PLS framework. Step~1 is
positioned as a direct enhancement to the existing \oldsystem{}
workflow: users run all 11 kernels as before, then apply
harmonization to produce an optimal, regime-specific blend.
Step~2 is for users who want to bypass the kernel computation
entirely and work directly with observational data.

% ============================================================
\section{Discussion}
\label{sec:discussion}

\paragraph{Step~1 as a bridge.}
The two-stage kernel harmonization step serves an important
bridging role: it demonstrates that the PLS/SIMCA/constraint
framework produces meaningful improvements on a problem the
community already cares about (interkernel spread), using tools
the community already has (the 11 \oldsystem{} kernel sets).
The data-driven regime discovery (11 kernel regimes via SIMCA)
reveals \emph{where} each kernel excels, providing diagnostic
value beyond the harmonized prediction itself. The four retune
approaches offer flexibility---users can select the method that
best suits their thermodynamic compliance requirements. This
lowers the barrier to adoption of the more radical Step~2.
Figure~\ref{fig:kernel_weights} shows both VIP scores and
regression coefficients, revealing which kernels are most
important and how they combine in the optimal blend.

\begin{figure}[t]
\centering
\includegraphics[width=0.75\textwidth]{figures/fig6_kernel_weights.pdf}
\caption{Kernel importance in the global PLS harmonization on
  real CERES + NCEP data. Left: Variable Importance in Projection
  (VIP) scores, which account for variance explained per PLS
  component---kernels with VIP $> 1$ (green, dashed threshold)
  contribute above average. Right: PLS regression coefficients
  (LW component); positive weights (green) contribute
  constructively, negative weights (red) are partially subtracted
  to correct collinear biases.}
\label{fig:kernel_weights}
\end{figure}

\paragraph{PLS diagnostics and HadGEM3-GA7.1 dominance.}
Figure~\ref{fig:diagnostics} provides standard PLS/SIMCA
diagnostic plots. The loading plot (panel~d) reveals a striking
structure: HadGEM3-GA7.1 sits isolated at loading distance 1.01
in the opposite quadrant from the other 10 kernels, which cluster
tightly at distances 0.27--0.35. This is a purely data-driven
result---HadGEM3-GA7.1 has 3.5$\times$ the prediction standard
deviation (116~\Wmsq{}) of any other kernel ($\sim$28--34~\Wmsq{}),
yielding 3.2$\times$ the covariance with CERES observations.
Since PLS component~1 maximizes covariance, HadGEM3-GA7.1
dominates: VIP~$= 2.49$ vs.~$0.55$--$0.77$ for the rest.

Paradoxically, HadGEM3-GA7.1 has the \emph{second-lowest}
correlation with CERES ($r = -0.69$; CAM3 leads at $r = -0.74$)
and the \emph{worst} MSE by 8$\times$ (16{,}867 vs.\
1{,}700--2{,}400). But it has the lowest bias ($+1.4$~\Wmsq{}
vs.\ $-2.3$ to $-4.1$ for others). PLS exploits this: HadGEM3
provides the largest ``lever arm'' for covariance projection,
and the remaining kernels correct its overestimation. An
ablation test confirms this interpretation: dropping HadGEM3
\emph{improves} $\Qsq$ by $+0.003$ ($0.548$ vs.\ $0.544$),
because the 3-component model wastes 2 components modeling
HadGEM3's outlier variance.

The score plot (panel~a) shows HadGEM3's regime (teal) centered
near the origin ($\bar{t}_1 = +1.1$, $\bar{t}_2 = -1.5$),
grounding the model, while other regimes project outward (CAM3 at
$t_1 = +72$, CAM5/HadGEM2 at $t_1 = -36$ to $-41$). Hotelling's
$T^2$ (panel~b) and DModX (panel~c) characterize model coverage
and X-space residuals respectively.

\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{figures/fig7_diagnostics.pdf}
\caption{PLS/SIMCA diagnostics on real CERES + NCEP test data.
  (a)~Score plot ($t_1$ vs.\ $t_2$) colored by kernel regime;
  HadGEM3-GA7.1 (regime~10) centers the model.
  (b)~Hotelling's $T^2$ distribution with 95\% $\chi^2$ limit.
  (c)~DModX per observation, colored by regime.
  (d)~Loading plot: HadGEM3-GA7.1 is isolated at distance 1.01;
  the remaining 10 kernels cluster at 0.27--0.35, revealing
  their high collinearity.}
\label{fig:diagnostics}
\end{figure}

\paragraph{Component selection.}
Figure~\ref{fig:component_sweep} shows $\Qsq$ as a function
of the number of PLS components. Component~1 captures 95.4\%
of X-variance and achieves $\Qsq = 0.523$ (dominated by
HadGEM3-GA7.1). Component~2 adds 3.5\% X-variance and $\Delta
\Qsq = +0.019$ (the HadGEM3 vs.\ others contrast). Component~3
captures CloudSat's unique variance ($p_3 = 0.96$) for $\Delta
\Qsq = +0.003$. Beyond 3 components, marginal $\Qsq$ gains fall
below 0.005 per component and approach zero at $A = 7$. Test
$\Qsq$ never falls below train $R^2$, confirming no overfitting
with 224{,}000 training samples and 11 features. We use $A = 3$
as a conservative default; cross-validated component selection is
left to future work.

\begin{figure}[t]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig8_component_sweep.pdf}
\caption{PLS component selection on real CERES + NCEP data.
  Left: train $R^2$ and test $\Qsq$ vs.\ number of components;
  both increase monotonically with no overfitting.
  Right: marginal $\Qsq$ gain per component. The first 3
  components (green) capture the dominant structure; components
  4--8 (gray) add $< 0.005$ each. Dashed red line marks $\Delta
  \Qsq = 0.001$.}
\label{fig:component_sweep}
\end{figure}

\paragraph{11 kernel regimes vs.\ 16 climate-state regimes.}
The data-driven 11 kernel regimes (one per kernel, assigned by
$\arg\min_k |{\Delta R^{(k)} - \Delta R^\text{obs}}|$)
outperform the 16 prescribed climate-state regimes
($4 \times 2 \times 2$: latitude $\times$ cloud $\times$
stability) on this data: the 11-regime global PLS achieves
$\Qsq = 0.544$ vs.\ $\Qsq = 0.452$ for 16-regime SIMCA-blended
PLS. The prescribed regimes are degenerate---most samples fall
into only 4 of 16 bins (no convective regimes)---while the
kernel regimes are well-distributed (10--17\% each for the top 5
kernels). Cross-tabulation reveals that HadGEM3-GA7.1 dominates
the subtropical-cloudy, midlatitude, and polar-clear regimes
(23--32\%), while CAM3, CloudSat, and BMRC take over in
polar-cloudy and equatorial domains---a physically sensible
pattern consistent with each model's radiative transfer
assumptions.

\paragraph{Why $\Qsq < 0$ for individual kernels?}
On real data, all 11 individual kernel predictions produce
$\Qsq < 0$ (best: CloudSat with $\Qsq = -3.90$), meaning they
are worse than predicting the mean $\Delta$OLR. This arises
because raw kernel predictions $\Delta R^{(k)} = \sum_p
K_T^{(k)}(p) \Delta T(p) + K_{Ts}^{(k)} \Delta T_s + \ldots$
contain substantial biases from: (1) differing radiative transfer
codes and vertical resolution (17--37 pressure levels across
kernels), (2) nearest-neighbor spatial regridding from kernel grids
(56--360 meridional points) to the 36$\times$36 observational grid,
and (3) linearization errors at large anomalies. That PLS
harmonization transforms these collectively biased predictions
into a skillful ensemble ($\Qsq = 0.554$) demonstrates the
power of the data-driven approach: the PLS regression coefficients
learn to correct systematic biases while preserving the
information content of each kernel.

\paragraph{Why $\Qsq = 0.704$ on real data (Step~2)?}
The gap between synthetic ($\Qsq = 0.967$) and real ($\Qsq =
0.704$) predictive skill reflects physical processes absent from
our linear framework: aerosol forcing, ocean heat transport,
dynamical feedbacks, and nonlinear interactions between feedback
components. The $\Qsq = 0.704$ nonetheless indicates that 70\% of
TOA flux variance is explained by the linear atmospheric state
model---consistent with the dominance of radiative feedbacks over
dynamical contributions at monthly--interannual timescales.

\paragraph{Step~2 feature importance and diagnostics.}
Figure~\ref{fig:step2_vip} decomposes the 27 atmospheric features by
Variable Importance in Projection (VIP) and PLS regression coefficients.
Temperature features at lower tropospheric levels (T$_{1000}$--T$_{850}$)
and surface temperature dominate the VIP ranking, consistent with the
Planck feedback being the strongest radiative response.  Humidity features
(q$_{1000}$--q$_{300}$) contribute secondary but physically meaningful
variance through the water vapour feedback.  Cloud fraction shows a
moderate VIP, reflecting the known large radiative sensitivity to cloud
changes.  The regression coefficients (panel~b) reveal the sign structure:
surface warming increases OLR (positive LW coefficient), while humidity
increases at lower levels reduce OLR through enhanced greenhouse trapping
(negative LW coefficient)---both physically consistent with radiative
transfer theory.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{figures/fig9_step2_vip.pdf}
\caption{Step~2 data-driven kernel: variable importance in projection (VIP)
  and PLS regression coefficients for 27 atmospheric features.  Panel~(a):
  VIP scores sorted by importance; the dashed line marks VIP~=~1 threshold.
  Blue bars: temperature levels; teal: humidity levels; amber: surface
  temperature; orange: cloud fraction.  Panel~(b): regression coefficients
  for the longwave ($\Delta R_\mathrm{LW}$) component, revealing the sign
  and magnitude of each feature's contribution to TOA flux prediction.
  Fitted on 13,219 real CERES+NCEP observations (2003--2017).}
\label{fig:step2_vip}
\end{figure}

Figure~\ref{fig:step2_diagnostics} presents PLS diagnostics for the
10-component Step~2 model.  The score plot (panel~a) colours observations
by surface temperature anomaly, revealing a clear temperature gradient along
the first latent variable---confirming that $t_1$ captures the dominant
Planck-driven radiative response.  Hotelling's $T^2$ (panel~b) and DModX
(panel~c) identify observations with unusual leverage or poor model fit,
respectively.  The loading plot (panel~d) shows the 27-feature positions in
latent space: temperature features cluster tightly (high collinearity across
pressure levels), humidity features form a separate cluster, and cloud
fraction sits isolated---consistent with the distinct physical mechanisms of
Planck, water vapour, and cloud feedbacks.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{figures/fig10_step2_diagnostics.pdf}
\caption{Step~2 PLS diagnostics (10 components, 27 atmospheric features,
  real CERES+NCEP data).  (a)~Score plot $t_1$ vs $t_2$, coloured by
  surface temperature anomaly.  (b)~Hotelling's $T^2$ distribution with
  95\% critical limit.  (c)~DModX (RMS X-residual) per observation,
  coloured by cloud fraction anomaly.  (d)~Loading plot showing the
  position of all 27 features in PLS latent space.}
\label{fig:step2_diagnostics}
\end{figure}

Figure~\ref{fig:step2_components} shows the component sweep for the Step~2
model.  The first component alone captures substantial $R^2$ and $Q^2$
(the Planck response), with diminishing marginal gains beyond
$\sim$5 components.  The marginal $\Delta Q^2$ plot (panel~b) shows
that components 1--5 each contribute meaningfully, while components beyond
10 provide negligible improvement.  The gap between train $R^2$ and test
$Q^2$ remains small, indicating no overfitting despite the 10-component
model.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig11_step2_component_sweep.pdf}
\caption{Step~2 component selection: $R^2$ (train) and $Q^2$ (test) as a
  function of the number of PLS components.  Left: cumulative performance;
  right: marginal $\Delta Q^2$ per added component.  The dashed vertical
  line marks $A=10$ (the value used).  Fitted on 13,219 real observations;
  tested on 3,305 holdout observations (2018--2020).}
\label{fig:step2_components}
\end{figure}

\paragraph{Constraint weight tuning.}
Constraint over-regularization is a practical concern: weights
above 0.5 destroy predictive skill ($\Qsq < 0.3$). We recommend
$\lambda_\text{SB} \in [0.05, 0.1]$ and $\lambda_\text{EC} \in
[0.01, 0.05]$ based on synthetic experiments. Cross-validated
constraint weight selection is left to future work.

\paragraph{PLS feedback decomposition.}
Individual feedback magnitudes from PLS are approximate because PLS
distributes sensitivity across correlated features. The
aggregated predictive quality ($\Qsq > 0.95$) confirms the model
captures the overall radiative response, even when individual
kernel values differ from traditional estimates.

\paragraph{Scalability.}
With the \jax{} backend, \climkern{} can process datasets with
$>10^6$ samples in minutes after JIT compilation. The modular
architecture supports incremental calibration.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}

We present \climkern{}, a two-step extension of \oldsystem{}
v1.2 that addresses the interkernel spread problem and
introduces data-driven kernel estimation:

\begin{enumerate}[noitemsep]
  \item \textbf{Step~1 (Two-Stage Kernel Harmonization):}
    Stage~1 preserves the \oldsystem{} kernels, fitting a global
    PLS and discovering 11 data-driven SIMCA regimes. Stage~2
    retunes a global PLS using the regime structure via four
    competing approaches (SIMCA-augmented features, regime-weighted
    PLS, iterative refinement, regime collapse), achieving
    $\Qsq = 0.806$ on synthetic benchmarks and $\Qsq = 0.554$ on
    real CERES + NCEP data with 76\% interkernel RMSE reduction.
    Individual kernels achieve $\Qsq < 0$ on real data, demonstrating
    the necessity of data-driven harmonization. A comprehensive
    $\Qsq$ dashboard and thermodynamic compliance analysis enable
    principled method selection. xESMF regridding maintains full
    backward compatibility with \oldsystem{} v1.2.
  \item \textbf{Step~2 (Data-Driven Extension):} The same
    PLS/SIMCA/constraint framework learns kernel sensitivities
    directly from atmospheric state observations, achieving
    $\Qsq = 0.704$ on real holdout data with physically correct
    feedback signs. A dual \jax{}/\numpy{} backend enables GPU
    acceleration with exact autodifferentiation gradients.
\end{enumerate}

Both steps share the same mathematical framework, physical
constraints, and state-classification machinery, demonstrating
that the constrained PLS approach is a general tool for
radiative kernel estimation---whether combining existing kernels
or learning new ones from scratch.

The package is validated with 57 tests (44 unit + 6 workflow + 9
scientific) at 100\% pass rate, including real observational data
from CERES EBAF-TOA Ed4.2 and NCEP/NCAR Reanalysis~1.

\paragraph{Code and data availability.}
\climkern{} is open-source at
\url{https://github.com/gogipav14/climkern} (branch
\texttt{claude/validate-jax-nipals-JsWVQ}). The merged CERES+NCEP
validation dataset can be regenerated using
\texttt{validate\_real\_data.py}.

% ============================================================
\section*{Acknowledgments}

We thank Tyler Janoski, Ivan Mitevski, Ryan Kramer, Michael
Previdi, and Lorenzo Polvani for their work on \oldsystem{} v1.2,
which provides the kernel repository and feedback computation
infrastructure upon which Step~1 is built. Development was
assisted by Claude (Anthropic).

% ============================================================
\bibliographystyle{apalike}
\begin{thebibliography}{99}

\bibitem[Andrews et~al., 2015]{Andrews2015}
Andrews, T., Gregory, J.~M., and Webb, M.~J. (2015).
The dependence of radiative forcing and feedback on evolving
patterns of surface temperature change in climate models.
\textit{J.~Climate}, 28(4):1630--1648.

\bibitem[Armour et~al., 2013]{Armour2013}
Armour, K.~C., Bitz, C.~M., and Roe, G.~H. (2013).
Time-varying climate sensitivity from regional feedbacks.
\textit{J.~Climate}, 26(13):4518--4534.

\bibitem[Dessler, 2010]{Dessler2010}
Dessler, A.~E. (2010).
A determination of the cloud feedback from climate variations over
the past decade.
\textit{Science}, 330(6010):1523--1527.

\bibitem[Janoski et~al., 2025]{Janoski2025}
Janoski, T.~P., Mitevski, I., Kramer, R.~J., Previdi, M., and
Polvani, L.~M. (2025).
ClimKern v1.2: a new Python package and kernel repository for
calculating radiative feedbacks.
\textit{Geoscientific Model Development}, 18:3065--3079.
\url{https://doi.org/10.5194/gmd-18-3065-2025}

\bibitem[Kalnay et~al., 1996]{Kalnay1996}
Kalnay, E., et~al. (1996).
The NCEP/NCAR 40-year reanalysis project.
\textit{Bull.~Amer.~Meteorol.~Soc.}, 77(3):437--472.

\bibitem[Loeb et~al., 2018]{Loeb2018}
Loeb, N.~G., et~al. (2018).
Clouds and the Earth's Radiant Energy System (CERES) Energy
Balanced and Filled (EBAF) top-of-atmosphere (TOA) Edition-4.0
data product.
\textit{J.~Climate}, 31(2):895--918.

\bibitem[Soden et~al., 2008]{Soden2008}
Soden, B.~J., Held, I.~M., Colman, R., Shell, K.~M., Kiehl,
J.~T., and Shields, C.~A. (2008).
Quantifying climate feedbacks using radiative kernels.
\textit{J.~Climate}, 21(14):3504--3520.

\bibitem[Tenenhaus, 1998]{Tenenhaus1998}
Tenenhaus, M. (1998).
\textit{La r\'{e}gression PLS: th\'{e}orie et pratique}.
\'{E}ditions Technip, Paris.

\bibitem[Wold, 1976]{Wold1976}
Wold, S. (1976).
Pattern recognition by means of disjoint principal components
models.
\textit{Pattern Recognition}, 8(3):127--139.

\bibitem[Wold et~al., 2001]{Wold2001}
Wold, S., Sj\"{o}str\"{o}m, M., and Eriksson, L. (2001).
PLS-regression: a basic tool of chemometrics.
\textit{Chemom.~Intell.~Lab.~Syst.}, 58(2):109--130.

\bibitem[Zelinka et~al., 2020]{Zelinka2020}
Zelinka, M.~D., et~al. (2020).
Causes of higher climate sensitivity in CMIP6 models.
\textit{Geophys.~Res.~Lett.}, 47(1):e2019GL085782.

\end{thebibliography}

\end{document}
